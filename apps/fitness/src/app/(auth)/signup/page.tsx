import { SignupForm } from '@/components/auth/SignupForm'

export const metadata = {
  title: 'Registrace | FitAdmin',
  description: 'Vytvořte si účet v FitAdmin',
}

export default function SignupPage() {
  return <SignupForm />
}
